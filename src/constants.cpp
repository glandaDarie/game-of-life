#include "../utils/constants.h"
#include <cmath>

namespace Constants {
    int NUM_GENERATIONS = 100;
    int BOARD_ROWS = 1000;
    int BOARD_COLUMNS = 1000;
    int CHUNK_SIZE = 3;
    const float EPSILON = 0.0001f;
}